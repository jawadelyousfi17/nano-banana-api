import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Nano Banana AI Image Generator',
  description: 'Generate stunning AI images using customizable prompt templates with Nano Banana API',
  keywords: 'AI, image generation, Nano Banana, artificial intelligence, creative tools',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
